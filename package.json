{
  "name": "m3u8-proxy-demo",
  "version": "2.0.0",
  "description": "M3U8流媒体代理服务器，支持广告过滤和路径重写",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "ui:install": "npm --prefix frontend install",
    "ui:dev": "npm --prefix frontend run dev",
    "ui:build": "npm --prefix frontend run build",
    "ui:preview": "npm --prefix frontend run preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["m3u8", "proxy", "streaming", "ad-filter"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "express-rate-limit": "^7.1.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}